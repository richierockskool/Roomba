/**
 * Platform name used in Homebridge config.json.
 */
export const PLATFORM_NAME = 'Roomba';

/**
 * Plugin package name.
 * Must exactly match the "name" property in package.json.
 */
export const PLUGIN_NAME = 'homebridge-roomba-pro';
